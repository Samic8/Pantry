type Item {
  id: ID! @unique
  name: String!
  maxOnHand: Int!
  unit: String!
  restocks: [Restock]
}

type Restock {
  date: DateTime!
  newOnHand: Int!
  previousRestock: Restock
  userEstimateRunOut: DateTime
  didRunOut: DateTime
  leftOverFromPrevious: Int
}